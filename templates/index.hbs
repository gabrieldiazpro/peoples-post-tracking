<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Peoples Post - Suivi de colis</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }

        :root {
            --primary: #2563EB;
            --primary-dark: #1E40AF;
            --gray-50: #f8fafc;
            --gray-100: #f1f5f9;
            --gray-600: #475569;
            --gray-800: #1e293b;
            --gray-900: #0f172a;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        .header {
            padding: 20px 40px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 12px;
            color: white;
            font-weight: 700;
            font-size: 24px;
        }

        .logo img {
            height: 40px;
        }

        .nav-links {
            display: flex;
            gap: 30px;
        }

        .nav-links a {
            color: rgba(255,255,255,0.9);
            text-decoration: none;
            font-weight: 500;
            transition: color 0.2s;
        }

        .nav-links a:hover {
            color: white;
        }

        .main {
            flex: 1;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 40px;
        }

        .container {
            max-width: 600px;
            width: 100%;
        }

        .hero-text {
            text-align: center;
            color: white;
            margin-bottom: 40px;
        }

        .hero-text h1 {
            font-size: 48px;
            font-weight: 800;
            margin-bottom: 16px;
            text-shadow: 0 2px 10px rgba(0,0,0,0.2);
        }

        .hero-text p {
            font-size: 20px;
            opacity: 0.9;
        }

        .tracking-box {
            background: white;
            border-radius: 24px;
            padding: 40px;
            box-shadow: 0 25px 50px rgba(0,0,0,0.25);
        }

        .tracking-box h2 {
            font-size: 24px;
            color: var(--gray-900);
            margin-bottom: 8px;
        }

        .tracking-box p {
            color: var(--gray-600);
            margin-bottom: 24px;
        }

        .input-group {
            display: flex;
            gap: 12px;
        }

        .input-group input {
            flex: 1;
            padding: 16px 20px;
            border: 2px solid var(--gray-100);
            border-radius: 12px;
            font-size: 16px;
            font-family: inherit;
            transition: border-color 0.2s;
        }

        .input-group input:focus {
            outline: none;
            border-color: var(--primary);
        }

        .input-group button {
            padding: 16px 32px;
            background: var(--primary);
            color: white;
            border: none;
            border-radius: 12px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: background 0.2s;
        }

        .input-group button:hover {
            background: var(--primary-dark);
        }

        .features {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 16px;
            margin-top: 32px;
        }

        .feature {
            text-align: center;
            padding: 20px;
            background: var(--gray-50);
            border-radius: 12px;
        }

        .feature-icon {
            font-size: 28px;
            margin-bottom: 8px;
        }

        .feature-title {
            font-weight: 600;
            color: var(--gray-800);
            font-size: 14px;
        }

        .footer {
            text-align: center;
            padding: 30px;
            color: rgba(255,255,255,0.7);
            font-size: 14px;
        }

        .footer a {
            color: white;
            text-decoration: none;
        }

        @media (max-width: 640px) {
            .header {
                padding: 20px;
            }

            .nav-links {
                display: none;
            }

            .hero-text h1 {
                font-size: 32px;
            }

            .tracking-box {
                padding: 24px;
            }

            .input-group {
                flex-direction: column;
            }

            .features {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <header class="header">
        <div class="logo">
            <img src="/assets/logo.png" alt="Peoples Post" onerror="this.style.display='none'">
            <span>Peoples Post</span>
        </div>
        <nav class="nav-links">
            <a href="#track">Suivi</a>
            <a href="/returns/a0000000-0000-0000-0000-000000000001">Retours</a>
            <a href="#contact">Contact</a>
        </nav>
    </header>

    <main class="main">
        <div class="container">
            <div class="hero-text">
                <h1>Suivez votre colis</h1>
                <p>Entrez votre numero de suivi pour connaitre l'etat de votre livraison</p>
            </div>

            <div class="tracking-box" id="track">
                <h2>Ou est mon colis ?</h2>
                <p>Saisissez votre numero de suivi ci-dessous</p>

                <form action="/t/" method="GET" onsubmit="return redirectToTracking(event)">
                    <div class="input-group">
                        <input type="text" id="trackingNumber" placeholder="Ex: 6A12345678901" required>
                        <button type="submit">Suivre</button>
                    </div>
                </form>

                <div class="features">
                    <div class="feature">
                        <div class="feature-icon">📍</div>
                        <div class="feature-title">Suivi en temps reel</div>
                    </div>
                    <div class="feature">
                        <div class="feature-icon">📧</div>
                        <div class="feature-title">Notifications</div>
                    </div>
                    <div class="feature">
                        <div class="feature-icon">↩️</div>
                        <div class="feature-title">Retours faciles</div>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <footer class="footer">
        <p>&copy; 2024 Peoples Post. Tous droits reserves. | <a href="#contact" id="contact">Contact</a></p>
    </footer>

    <script>
        function redirectToTracking(e) {
            e.preventDefault();
            const trackingNumber = document.getElementById('trackingNumber').value.trim();
            if (trackingNumber) {
                window.location.href = '/t/' + encodeURIComponent(trackingNumber);
            }
            return false;
        }
    </script>
</body>
</html>
